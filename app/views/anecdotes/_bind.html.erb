<%= form_for(anecdote) do |f| %>
	Bind to chapter n°
	<%= collection_select(:anecdote, :chapter_id, Chapter.all, :id, :number, prompt: true) %>
  	<%= f.submit "Bind to", class: "btn btn-small btn-primary" %>
<% end %>